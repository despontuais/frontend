import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#99c425', // Customize com sua cor preferida
    },
    secondary: {
      main: '#e3efc3', // Customize com sua cor preferida
    },
    
  },
});

export default theme;
