import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#157145', // Customize com sua cor preferida
    },
    secondary: {
      main: '#157145', // Customize com sua cor preferida
    },
    
  },
});

export default theme;
